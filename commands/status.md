---
description: Check Claude Narrator daemon status
allowed-tools: Bash
---

# Claude Narrator Status

Run the following command and show the output to the user:

```bash
claude-narrator status
```

If the daemon is not running, ask if they want to start it:
```bash
claude-narrator start
```
